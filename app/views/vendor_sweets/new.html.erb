
<h1>Set a Sweets Price</h1>
<%= form_with model: @vendor_sweet do |f| %>
<%= f.label :price %>
<%= f.text_field :price %>
<%= f.collection_select :vendor_id, @vendors, :id, :name %>
<%= f.collection_select :sweets_id, @sweets, :id, :name %>
<%= f.submit %>
<% end %>